Codey v0.9.x → v1.0.0 Task List

🔴 P1 — Stability (Do First, Highest Impact)
1. Fix brittle JSON parser (core/agent.py lines 60-90) [DONE]
   - Replace regex depth-counting with a proper lazy JSON extractor
   - This breaks the agent loop on malformed output — most frequent failure mode
2. Fix patch_file collision risk (tools/patch_tools.py) [DONE]
   - Add uniqueness check: if old_str appears more than once, reject and ask model to provide more context
   - Add line count to error message so model knows what it's working with
3. Add .codeyignore [DONE]
   - Prevent auto_load_from_prompt and /load from reading .env, *.pem, *.key, secrets
   - Simple pattern file in project root, checked before any file load

🟠 P2 — Security (High Value, Manageable Effort)
4. Harden shell blacklist → allowlist approach (tools/shell_tools.py) [DONE]
   - Replace DANGEROUS_PATTERNS blacklist with a warning + confirmation for any command containing rm, curl, wget, chmod, dd, mkfs
   - Add proot sandbox option for Termux
5. Mask secrets in session saves (core/sessions.py) [DONE]
   - Before saving history to JSON, scan for patterns matching API keys, passwords, tokens and redact them
6. Workspace root restriction [DONE]
   - Add WORKSPACE_ROOT = os.getcwd() at startup
   - Block file reads/writes outside workspace unless absolute path is explicitly confirmed

🟡 P3 — Features (Medium Effort, High Payoff)
7. Better patch_file with context lines [DONE]
   - Require model to include 2 lines before and after the change for uniqueness
   - Match on full context block, replace only the target lines
8. Move hardcoded paths to config (core/inference.py) [DONE]
   - llama-server path and model path into utils/config.py
   - Auto-detect via which llama-server fallback
9. Auto-commit after successful tasks (/git enhancement) [DONE]
   - After tests pass or task completes cleanly, offer git add -A && git commit -m "Codey: <task summary>"
   - Already have /git command, just wire it to task completion
10. Repo map (lightweight) [DONE]
    - On startup, scan project for def, class, import lines only (no full file content)
    - Inject as compressed ## Project Structure block — like Aider's repo map but without Tree-sitter

🟢 P4 — Polish (Low Effort, Nice to Have)
11. Update README.md with current v0.9.0 features, commands, and architecture
12. Fix "Final Answer:" prefix still leaking into task checklist results
13. Add --no-plan flag to bypass orchestrator even for complex tasks [DONE]
14. /ignore command to add patterns to .codeyignore from chat [DONE]

🔵 P5 — Long Term (Big Effort, Big Payoff)
15. Vector DB / RAG — chromadb for whole-repo semantic search
16. Multi-model routing — local for simple, API for complex
17. Tree-sitter repo map — definition-level context without full file loading

Suggested order: P1 items 1→2→3, then P3 item 8 (quick win), then README.
