# Core Kuza runtime (Termux and desktop)
rich>=14.0.0
numpy>=1.24.0
watchdog>=3.0.0
aiohttp>=3.9.0
requests>=2.31.0
beautifulsoup4>=4.12.0
httpx>=0.27.0
pyyaml>=6.0
filelock>=3.13.0
tqdm>=4.65.0
pexpect>=4.9.0

# Optional acceleration: hnswlib>=0.7.0
# Kuza falls back to NumPy search when hnswlib is unavailable.
